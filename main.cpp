#include <stdio.h>
#include <graphics.h>
#include "calculate-utility.h"

// configure
int windowWidth = 1000;
int windowHeight = 500;
char windowTitle[] = "DO AN CO SO - HO VAN VY";

int main() {
    getch();
    closegraph();
    return 0;
}
